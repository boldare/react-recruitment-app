import { cardWidth } from '../../theme/dimensions';

const styles = () => ({
  dialogContent: {
    width: cardWidth,
    display: 'flex',
    alignItems: 'center',
    justifyContent: 'center'
  }
});

export default styles;
