import { actionButton } from '../../theme/dimensions';

const styles = theme => ({
  snackbar: {
    margin: theme.spacing.unit
  },
  close: {
    width: actionButton,
    height: actionButton
  }
});

export default styles;
